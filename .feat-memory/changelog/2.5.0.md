---
version: 2.5.0
date: 2026-06-30
---

# 2.5.0 — 2026-06-30

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Adicionado

- **Aviso de frontmatter stale** no `deploy` e no `audit`: paths `.agent-memory/` legados, chave `references.state`, `budgets.state_max_bytes` ou URL de `methodology` numa versão antiga viram ⚠ no deploy/dry-run e warning durável no audit. Fecha do lado do consumidor o ponto-cego que recorreu da 2.2.x — o frontmatter é human-owned, o deploy não o corrige. (F-0041, ADR-0049)

## Mudado

- **`deploy --dry-run` mais fiel** (feedback do tensegrams): hooks e `merge.ours.driver` já presentes agora reportam "já instalado"/"já configurado" em vez de sempre "instalaria/configuraria"; o rodapé separa `N arquivo(s)` de `M ação(ões) de ambiente` (não mistura escopos). (F-0040)

## Corrigido
