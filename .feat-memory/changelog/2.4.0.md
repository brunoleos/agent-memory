---
version: 2.4.0
date: 2026-06-30
---

# 2.4.0 — 2026-06-30

# Não-lançado

Trabalho concluído mas ainda não tagueado. Cada entrada referencia as
features e decisões que toca (`F-NNNN` / `ADR-NNNN`); o orçamento de
retomada é derivado dessas referências. Vazio = nada em voo.

## Adicionado

- **`feat-memory deploy --dry-run`**: reporta o que o deploy criaria/atualizaria (arquivos, hooks, git config) sem escrever um byte. A garantia anti-mutação é coberta por teste de regressão byte-idêntico. Promove a ideia `deploy-dry-run` do funil. (F-0040)

## Mudado

- `deploy` de skills/subagents agora compara conteúdo e vira no-op quando idêntico ("já em dia") em vez de reescrever sempre — torna o `--dry-run` honesto e o deploy idempotente de fato. (F-0040)

## Corrigido

- Aceitação A1 de F-0038 falava em "migra um suggestions.md legado", migração removida na 2.3.0 — corrigida para refletir o deploy atual. (F-0038)
